enum FlashMode { auto, on, off, always }

enum CameraPosition { front, back }

enum GridType { none, ruleOfThirds, goldenRatio, symmetry }

enum CameraMode { photo, video, pro }

enum CameraViewState { initializing, ready, capturing, error }
